# StockDash 개발 지침

## 한국투자증권 Open API 참고 자료

한국투자증권(KIS) Open API 연동을 설계하거나 수정할 때 공식 샘플 저장소
[한국투자증권 open-trading-api](https://github.com/koreainvestment/open-trading-api)를 우선 참고한다.
에이전트용 탐색 안내는 [llms.txt](https://github.com/koreainvestment/open-trading-api/blob/main/llms.txt),
기능별 최소 예제는 `examples_llm/`, 통합 사용 예제는 `examples_user/`에 있다.
인증, 국내주식 시세 및 잔고 기능은 해당 경로의 최신 예제와
[KIS Developers](https://apiportal.koreainvestment.com/)의 최신 API 문서를 함께 확인한다.
샘플 저장소는 변경될 수 있으므로 엔드포인트, 거래 ID, 실전/모의 구분, 요청·응답 필드,
호출 제한을 기억에 의존하여 확정하지 않는다.

## StockDash에 적용할 원칙

- KIS 연동은 사용자가 자신의 계좌 정보를 조회하도록 요청한 범위에서만 구현한다.
  주문·정정·취소 등 거래 기능을 새로 추가하거나 자동 실행하지 않는다.
- 앱의 기존 결산 재무 데이터는 DART, 현재 주식시세는 금융위원회 공공데이터 API를 사용한다.
  KIS 계좌/잔고 데이터와 공시·시세 데이터의 출처 및 기준 시각을 화면에서 구분한다.
- 앱 키, 앱 시크릿, 접근 토큰, 계좌번호는 서버 환경 변수 또는 Streamlit Secrets에만 둔다.
  코드, 로그, 테스트 데이터, 공개 수집본, Git 커밋에 비밀값을 넣지 않는다.
- 실전/모의 환경을 명확히 분리하고, 인증 만료·재발급·오류 응답을 처리한다.
  키가 없거나 인증에 실패하면 계좌 수치를 추측하거나 샘플 수치를 실제 데이터처럼 표시하지 않는다.
- API 변경 시 공식 예제와 문서를 다시 확인하고, 외부 API 호출은 테스트에서 모킹한다.
  실제 연결 성공은 사용자 환경에서 인증 응답과 조회 결과로 별도 확인한다.

## KRX OpenAPI 연동

- [KRX Data Marketplace Open API](https://openapi.krx.co.kr/)의 서비스 목록과 개발 명세서를 기준으로 구현한다.
- 월말 시가총액 산출에는 승인된 유가증권·코스닥 일별매매정보를 우선 검토한다. 각 월의 마지막 실제 거래일과 종목코드, 종가, 상장주식수, 시가총액의 필드·단위를 확인하고 2024-01~2025-12의 24개 월을 검증한다.
- 인증키 `KRX_AUTH_KEY`는 서버 Secrets에서 읽고 요청 헤더의 `AUTH_KEY`로만 전달한다. 키 발급과 개별 서비스 활용 승인은 별도이므로 승인되지 않은 API의 결과를 정상 자료로 표시하지 않는다.
- KRX 서비스의 URL·요청 필드·응답 형식과 이용조건은 구현 시 최신 공식 명세서에서 확인한다. 연결되지 않으면 v2.2의 월말 시가총액 배수를 계산하지 않고 필요한 데이터를 명시한다.
